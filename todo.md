todo.md

[x] 下载的时候把提示词整合到图片里面去，成为图片的元信息，带有原信息的图片拖进来的时候，也可以在提示词区域直接展示出提示词
[x] 搜索功能
[x] 相册模式-点击之后可以全屏展示所有历史生成的图片
[x] 官方支持高清格式下载
[x] 逆向按钮合并到底部成为展开的菜单，参考生成图片的张数。
[x] 相册里面新增加一列，相当于是看原图的模式。每选择一个原图，可以看到基于这个原图生成出来的所有生成图
[x] 提示词这里增加一个智能的分析，把有差异的部分文字展示出来
[x] 输入意见之后一种是修改提示词，一种是直接生成图片。
[x] key 支持多种代理配置。参考 chatwise。左侧是一个列表，选择官方还是自定义，右侧输入地址并输入 KEY，底部还可以设置模型的信息，比如：模型展示的名称是什么 模型的 ID 是什么
[x] 相册模式和大图模式下也需要支持删除图片，只是不需要强化该功能，也支持切换 对比模式查看。
[x] 聚焦在下面的文本输入框的时候，粘贴图片，直接粘贴图片的话就相当于把图片放到参考区域。
[x] @符号这个按钮位置一直在，哪怕新建任务。无法操作，那也只是状态变成禁用。
[x] 历史记录这里，右键支持将图片添加到对比模式。
[x] 最近的修改日志 每次 git commit 的时候，自动记录
[x] 历史记录这里的“右键添加到对比”功能可以进一步简化。当前右键点击后只有一个功能，所以不需要再弹出子菜单了，直接右键时就将其添加到对比中，这样操作更简化。
[x]之前的对比模式是通过右键添加，添加的部分是放到左边作为对比的对象，对吗？左键操作是选择当前激活的图放到右边，而对比的对象则是放到左边。实现的右键添加对比模式好像是错的，需要重新实现。

[ ] 前面相册详情查看大图的时候，我说的对比模式其实是指和原图的对比模式，而不是指添加到编辑模式里面去做对比。本质上就是要不要展示原图。
[ ] 详情页统一整理建议
• 现状：编辑模式包含“对比详情”与“单图详情”。
• 目标：将编辑模式的两种详情与相册详情进行统一。
• 方案：建议单图详情与相册详情共用组件，实现逻辑与视觉的一致性。
[ ] 支持多线程：当前的提示词提交之后，在生成的过程中，不影响新建任务继续提交